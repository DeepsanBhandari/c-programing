  #include<stdio.h>
   int main()
{
    int a,b,sum;
      printf("Enter any two numbers\n");
         scanf("%d%d",&a,&b);                      //  use  %d for integer input
           sum=a+b;
              printf("sum of two number is = %d",sum);   //no & symbol is used before sum                           
                 return 0;
}