#include <stdio.h>
#include<conio.h>
  int main() {
    int x, y, sum;
      printf("Enter the first number: ");
        scanf("%d", &x);                                 // Use %d for integer input
          printf("Enter the second number: ");
            scanf("%d", &y);                                  // Use %d for integer input
              sum = x + y;
                printf("Sum of two numbers = %d\n", sum);        // Use %d for integer output
                   return 0;                                            // Return 0 to indicate successful completion
}
