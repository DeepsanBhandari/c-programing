#include<stdio.h>
   float main()
{
    float Area, r;
      printf("Enter the radius \n");
        scanf("%f",&r);
          Area=(3.1415 * (r*r));
            printf("the area of circle is %f",Area);
                return 0;
}2